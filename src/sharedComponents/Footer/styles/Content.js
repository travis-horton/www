import React from 'react';
import styled from 'styled-components';

const StyledFooterContent = styled.small`
  margin: 0;
  padding: 15px;

  &:hover {
    cursor: default;
  }
`;

export default StyledFooterContent;
