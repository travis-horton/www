version: "3.8"
services:
  web:
    container_name: web
    build: .
    networks:
      - "net"
    environment:
      - VIRTUAL_PORT=80
      - VIRTUAL_HOST=kiddspazz.com
      - LETSENCRYPT_HOST=kiddspazz.com

networks:
  net:
    external:
      name: net
